<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body>

		<header>
		    <nav>
                <a href="{{ '/' | url }}" class="brand"><span>{{ metadata.title }}</span></a>
                {% include "components/nav.njk" %}
			</nav>

            <div class="heading">
                <h1>{{ title }}</h1>
            </div>
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
